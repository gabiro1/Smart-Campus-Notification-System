import express from 'express';
const router = express.Router();

// Settings routes are handled by admin controller
// This file exists for potential future standalone settings routes

export default router;